# types/

Global TypeScript interfaces and type aliases shared across the project.

Import from here rather than defining the same shape in multiple files.
Database row types (generated from Supabase) should also live here once the schema is set.
