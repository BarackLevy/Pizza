# components/ui/

Reusable UI primitives: Button, Card, Input, Badge, etc.

Rules:
- No business logic or data fetching here — pure presentation.
- Each component should be self-contained with its own types.
- Domain-specific components (e.g. MenuCard, OrderForm) live in `app/` alongside their routes.
